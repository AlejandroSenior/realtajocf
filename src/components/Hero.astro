<section>
  <div class="bg-primary flex flex-col items-center justify-center py-20">
    <h1 id="team-name" class="text-white text-center">Real Tajo C.F.</h1>

    <img id="crest" src="/escudo-without-bg.avif" alt="Escudo Real Tajo CF" class="w-21 md:w-36 mt-7" />
  </div>
</section>

<style>
  #team-name {
    animation-duration: 3s;
    animation-name: slide-in;
    color: var(--color-primary);
    -webkit-text-stroke-width: clamp(1px, 2px, 4px);
    -webkit-text-stroke-color: var(--color-secondary);
    font-size: clamp(33px, 5vw, 120px);
  }

  #crest {
    animation-duration: 3s;
    animation-name: slide-in;
  }

  @media (width <= 768px) {
    #team-name {
      -webkit-text-stroke-width: 0.7px;
    }
  }

  @keyframes slide-in {
    from {
      transform: translateY(2em);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
</style>
